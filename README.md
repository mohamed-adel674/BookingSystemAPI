🚀 Booking System API (نظام الحجوزات المتكامل)هذا المستودع يحتوي على الـ Backend API لنظام حجوزات متكامل (مثل حجز قاعات، عيادات، أو خدمات) مبني باستخدام Laravel و Laravel Sanctum.🌟 الميزات الرئيسية (Core Features)مصادقة عبر الـ API (Sanctum): تسجيل دخول/تسجيل وخروج باستخدام Token Authentication.نظام صلاحيات (Middleware): فصل واضح بين صلاحيات admin و customer.إدارة الموارد (CRUD): التحكم في إضافة وتعديل وحذف الموارد المتاحة للحجز.إدارة التوافر (Availability): تحديد جدول العمل الأسبوعي والاستثناءات الزمنية لكل مورد.منطق الجدولة المعقد: حساب الفترات الزمنية المتاحة فعليًا بعد طرح الحجوزات المتعارضة (Conflict Check).الإشعارات الآلية (Queue/Events): إرسال تأكيدات وإلغاءات الحجز عبر البريد الإلكتروني بشكل غير متزامن.المهام المجدولة (Scheduler): إرسال تذكيرات آلية قبل 24 ساعة من موعد الحجز.🛠️ الإعداد والتشغيل (Setup & Installation)اتبع الخطوات التالية لتشغيل المشروع على جهازك المحلي.1. المتطلبات (Prerequisites)PHP >= 8.2ComposerMySQL DatabaseGit2. استنساخ المستودع (Clone Repository)Bashgit clone <URL_المستودع_هنا>
cd BookingSystemAPI
3. تثبيت الاعتماديات (Dependencies)Bashcomposer install
4. إعداد ملف البيئة (.env)قم بإنشاء نسخة من الملف env.example وسمها .env:Bashcp .env.example .env
قم بتعديل المتغيرات التالية في ملف .env:VariableValue (Example)DescriptionDB_DATABASEbooking_api_dbاسم قاعدة البيانات الخاصة بك.DB_USERNAMErootاسم المستخدم لقاعدة البيانات.DB_PASSWORDsecretكلمة المرور لقاعدة البيانات.MAIL_MAILERsmtp / logاستخدم log للتطوير و smtp للإنتاج.توليد مفتاح التطبيق:Bashphp artisan key:generate
5. تشغيل Migrationsنفذ الـ Migrations لإنشاء الجداول في قاعدة البيانات:Bashphp artisan migrate
6. تشغيل الخادم وتجهيز الـ Queueلتشغيل الـ API:Bashphp artisan serve
يجب تشغيل عامل الـ Queue بشكل منفصل لمعالجة رسائل البريد الإلكتروني والتأكيدات (ضروري لاختبار الإشعارات):Bashphp artisan queue:work
🔑 الـ Endpoints والـ Authenticationجميع نقاط النهاية تبدأ بـ /api/. يجب إرسال التوكن في الـ Header لجميع المسارات المحمية.Header KeyHeader ValueAuthorizationBearer <your_sanctum_token>1. مسارات المصادقة (Auth Routes)MethodEndpointDescriptionPOST/api/registerتسجيل مستخدم جديد (customer افتراضي).POST/api/loginتسجيل الدخول والحصول على الـ Token.POST/api/logoutتسجيل الخروج وإلغاء صلاحية التوكن الحالي.2. إدارة الموارد والتوافر (Admin Only)ملاحظة: تتطلب صلاحية admin في الـ Middleware. يجب تعديل حقل role في جدول users إلى admin للاختبار.ResourceMethodEndpointDescriptionResourcesGET/api/resourcesعرض جميع الموارد.ResourcesPOST/api/resourcesإنشاء مورد جديد.AvailabilitiesPOST/api/availabilitiesتحديد أوقات توافر المورد (day_of_week, start_time, end_time).3. منطق الحجز والجدولة (Customer/Auth)MethodEndpointDescriptionGET/api/available-slotsالأهم: عرض الفترات المتاحة لمورد معين. يتطلب resource_id, start_date, end_date, duration_minutes.POST/api/bookingsإنشاء حجز جديد. يتم تطبيق منطق التوافر والتضارب.GET/api/bookingsعرض جميع حجوزات المستخدم الحالي.PATCH/api/bookings/{id}تعديل حالة الحجز (عادةً للإلغاء: {"status": "cancelled"}).⏰ إعداد الـ Scheduler (المهام المجدولة)لتمكين إرسال التذكيرات يوميًا (مثل أمر bookings:send-reminders):في ملف app/Console/Kernel.php: تم تحديد الأمر ليتم تشغيله يوميًا في وقت معين.على الخادم (الإنتاج): يجب إعداد Cron Job لتشغيل أمر Laravel Scheduler كل دقيقة:Bash* * * * * cd /path/to/your/project && php artisan schedule:run >> /dev/null 2>&1
